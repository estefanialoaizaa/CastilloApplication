# Configuraci\u00f3n de MySQL - CORREGIR PUERTO A 3307 SI ES NECESARIO VERIFICAR EN XAMPP
spring.datasource.url=jdbc:mysql://localhost:3307/demon_slayer_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging - CORREGIR PAQUETE
logging.level.CS2.CastilloApplication=DEBUG

# Server
server.port=8080

# Deshabilitar open-in-view para evitar warnings
spring.jpa.open-in-view=false